/** Stable FlatList keys — RN forbids changing numColumns on one instance. */
export const CREATOR_PROFILE_FLATLIST_KEYS = {
  collections: 'creator-profile-collections',
  products: 'creator-profile-products',
} as const;
