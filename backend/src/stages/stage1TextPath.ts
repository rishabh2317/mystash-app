/**
 * Stage 1 — metadata + transcript → ContextBuilder → Reasoner → Validator → Ranker.
 * Implemented inside `orchestrator.ts` (`runProgressiveIngestPipeline`).
 */
export { stagePass } from './gate';
