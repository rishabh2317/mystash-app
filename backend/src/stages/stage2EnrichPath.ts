/**
 * Stage 2 — adaptive frames + Media Understanding → ContextBuilder → Reasoner → Validator → Ranker.
 * Implemented inside `orchestrator.ts` when Stage 1 gate fails.
 */
export { needsStage2Enrichment } from './gate';
